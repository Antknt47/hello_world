#include <stdio.h>

// 长方形结构体
struct rect{
    double l; // 长
    double h; // 高
}

int main(int argc, char *argv[])
{
    struct rect_input = {3.0, 4.0}; // 长方形
    double s = how_big(&rect_input); // 计算长方形面积
    printf("the rect is %lf m^2\n", s); // 输出长方形面积
}

// 需要补全的函数
... how_big(...)
{
    
}